# version: '3.8' # Dihapus karena sudah obsolete di Docker Compose v2+

services:
  db:
    image: postgres:16-alpine  # Menggunakan versi lokal yang tersedia (15 & 16 kompatibel)
    container_name: antrian_db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-antrian_spmb}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: antrian_app
    restart: always
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=postgres://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@db:5432/${DB_NAME:-antrian_spmb}?sslmode=disable
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
      - APP_URL=${APP_URL} # Gunakan IP Host Anda di sini (misal: http://192.168.1.10:3000)
    depends_on:
      - db

volumes:
  postgres_data:
